import express from 'express'
//  Import here your controller like: import { getToken } from './controller'

const apiRouter = express.Router()

export function routes() {
  // Add here your controller functions like: apiRouter.post('/token', getToken)
  return apiRouter
}
